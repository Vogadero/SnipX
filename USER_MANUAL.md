# SnipX 用户手册

> 版本：v1.1.5
> 更新日期：2026-06-30

---

## 目录

1. [快速开始](#快速开始)
2. [基础功能](#基础功能)
3. [高级功能](#高级功能)
4. [快捷键](#快捷键)
5. [设置说明](#设置说明)
6. [常见问题](#常见问题)

---

## 快速开始

### 安装

1. 下载 `SnipX.exe`
2. 双击运行，程序将在系统托盘显示图标
3. 首次运行会自动创建配置文件

### 第一次截图

1. 按下 `Alt+X`（默认快捷键）
2. 鼠标拖拽选择截图区域
3. 松开鼠标确认选区
4. 在编辑器中添加标注
5. 按 `Ctrl+S` 保存或 `Ctrl+C` 复制

---

## 基础功能

### 1. 截图

#### 触发方式
- **快捷键**：按 `Alt+X`
- **托盘图标**：双击系统托盘图标
- **右键菜单**：右键托盘图标 → 截图

#### 选区操作
- **自由选区**：鼠标拖拽划选任意矩形区域
- **窗口检测**：移动鼠标到窗口边界自动识别
- **精确调整**：
  - 方向键：移动选区 1 像素
  - Shift+方向键：移动选区 10 像素
- **确认选区**：按 `Enter` 或松开鼠标
- **取消截图**：按 `Esc`

#### 辅助功能
- **放大镜**：鼠标周围显示 8x 放大区域
- **尺寸显示**：实时显示选区宽度和高度（像素）
- **多显示器**：自动支持多屏幕环境

### 2. 标注工具

截图完成后进入编辑器，提供 10 种标注工具：

#### 矩形工具
- 绘制矩形标注框
- 支持描边颜色和粗细调整
- 可拖拽移动和调整大小

#### 椭圆工具
- 绘制椭圆/圆形标注
- 支持颜色和粗细设置

#### 箭头工具
- 绘制箭头指示
- 多种箭头样式可选
- 支持单箭头和双箭头

#### 铅笔工具
- 自由手绘线条
- 可选颜色和粗细
- 适合圈画重点

#### 直线工具
- 绘制直线段
- 支持颜色和粗细

#### 文字工具
- 添加文字标注
- 支持字体、字号、颜色设置
- 可拖拽移动文字框

#### 序号标签工具
- 添加自动递增的序号标签
- 适合标注步骤和说明顺序

#### 高亮工具
- 使用半透明颜色突出重点区域
- 适合强调文本或界面区域

#### 马赛克工具
- 矩形区域像素化模糊
- 用于遮挡敏感信息
- 块大小可调

#### 模糊工具
- 高斯模糊效果
- 柔和的遮挡方式

### 3. 编辑操作

#### 撤销/重做
- `Ctrl+Z`：撤销上一步操作
- `Ctrl+Y`：重做已撤销的操作
- 支持多步撤销

#### 删除标注
- 选中标注对象
- 按 `Delete` 键删除

#### 调整标注
- 点击标注对象选中
- 拖拽移动位置
- 拖拽角点调整大小

### 4. 保存和导出

#### 保存到本地
- 按 `Ctrl+S` 打开保存对话框
- 支持格式：PNG、JPG、BMP
- JPG 可调质量（1-100）
- 文件名自动添加时间戳

#### 复制到剪贴板
- 按 `Ctrl+C` 复制截图
- 可直接粘贴到其他应用
- 双击图片区域快速复制并退出

#### 自动保存
- 在设置中启用自动保存
- 截图自动保存到指定目录
- 无需手动选择路径

---

## 高级功能

### 1. 取色器

#### 启动取色器
- 托盘菜单 → 取色器
- 或在截图模式下按 `R` 键

#### 使用方法
- 移动鼠标到目标颜色
- 查看放大镜和颜色信息
- 点击鼠标复制颜色值

#### 颜色格式
- **HEX**：#RRGGBB（点击复制）
- **RGB**：rgb(r, g, b)（按 2 复制）
- **HSL**：hsl(h, s%, l%)（按 3 复制）
- 按 `R` 或 `H` 键切换显示格式

### 2. 贴图功能

#### 创建贴图
- 截图后点击"贴图"按钮
- 截图将钉在桌面上

#### 贴图操作
- **移动**：拖拽贴图
- **缩放**：鼠标滚轮
- **透明度**：右键 → 透明度 → 选择档位
  - 100%（不透明）
  - 75%
  - 50%
  - 25%（半透明）
- **鼠标穿透**：右键 → 鼠标穿透
- **关闭**：右键 → 关闭

#### 剪贴板贴图
- 复制图片到剪贴板
- 托盘菜单 → 剪贴板贴图
- 图片将钉在桌面上

### 3. 录屏功能

#### 开始录制
- 托盘菜单 → 录屏
- 选择录制区域
- 点击开始录制

#### 停止录制
- 点击停止按钮
- 或按快捷键停止
- 自动生成 GIF 文件

### 4. 滚动截图

#### 使用场景
- 截取长网页
- 截取长文档
- 截取聊天记录

#### 操作步骤
- 托盘菜单 → 滚动截图
- 选择起始区域
- 程序自动滚动并拼接
- 完成后保存长图

### 5. OCR 文字识别

#### 识别文字
- 截图后点击"OCR"按钮
- 自动识别图片中的文字
- 文字复制到剪贴板

#### 支持语言
- 中文（简体/繁体）
- 英文
- 数字和符号

### 6. 图片上传

#### 上传到图床
- 截图后点击"上传"按钮
- 自动上传到配置的图床
- 链接复制到剪贴板

#### 支持图床
- 自定义图床 API
- 配置在设置中

---

## 快捷键

### 全局快捷键
| 快捷键 | 功能 |
|--------|------|
| `Alt+X` | 启动截图 |

### 截图模式
| 快捷键 | 功能 |
|--------|------|
| `Esc` | 取消截图 |
| `Enter` | 确认选区 |
| `↑↓←→` | 移动选区 1px |
| `Shift+↑↓←→` | 移动选区 10px |
| `R` / `H` | 切换取色模式 |

### 编辑器
| 快捷键 | 功能 |
|--------|------|
| `Ctrl+S` | 保存截图 |
| `Ctrl+C` | 复制到剪贴板 |
| `Ctrl+Z` | 撤销 |
| `Ctrl+Y` | 重做 |
| `Delete` | 删除选中标注 |
| `Esc` | 关闭编辑器 |
| `双击` | 复制并退出 |

---

## 设置说明

### 基本设置

#### 启动模式
- **驻留托盘**：启动后在托盘等待
- **启动即截图**：启动后直接进入截图模式

#### 开机自启
- 勾选后随 Windows 启动
- 取消勾选则不自动启动

#### 托盘图标
- 显示/隐藏系统托盘图标
- 隐藏后仅通过快捷键使用

### 保存设置

#### 默认格式
- PNG：无损压缩，文件较大
- JPG：有损压缩，文件较小
- BMP：无压缩，文件最大

#### 默认保存路径
- 设置保存对话框的初始目录
- 点击"浏览"选择文件夹

#### JPG 质量
- 范围：1-100
- 推荐：80-95
- 质量越高文件越大

#### 文件名前缀
- 自定义文件名前缀
- 默认：截图
- 示例：截图_20260515_143022.png

#### 自动保存
- 启用后截图自动保存
- 无需手动选择路径
- 保存到指定的自动保存目录

### 标注设置

#### 默认颜色
- 点击颜色块选择
- 新标注使用此颜色

#### 默认粗细
- 设置描边粗细
- 范围：1-10 像素

---

## 常见问题

### Q1: 热键冲突怎么办？
**A:** 在设置中修改全局热键，避免与其他软件冲突。

### Q2: 截图后编辑器没有打开？
**A:** 检查是否有其他窗口遮挡，或重启程序。

### Q3: 保存的图片在哪里？
**A:** 
- 手动保存：在保存对话框中选择的位置
- 自动保存：设置中配置的自动保存目录
- 默认：`我的文档\图片\Screenshots`

### Q4: 如何卸载？
**A:** 
1. 右键托盘图标 → 退出
2. 删除 SnipX.exe
3. 删除配置文件：`%APPDATA%\SnipX`

### Q5: 支持哪些系统？
**A:** Windows 7/8/8.1/10/11（32位和64位）

### Q6: 截图模糊怎么办？
**A:** 
- 检查显示缩放设置
- 在高DPI下可能需要调整
- 确保使用PNG格式保存

### Q7: 内存占用高吗？
**A:** 
- 空闲时：< 10 MB
- 截图时：< 50 MB
- 非常轻量

### Q8: 可以截取游戏画面吗？
**A:** 
- 窗口模式游戏：支持
- 全屏游戏：部分支持
- DirectX游戏：可能不支持

### Q9: 如何反馈问题？
**A:** 
- GitHub Issues
- 邮件联系
- 附带日志文件：`%APPDATA%\SnipX\snipx.log`

### Q10: 是否收费？
**A:** 完全免费，开源软件（MIT许可证）

---

## 技巧和窍门

### 1. 快速截图工作流
```
Alt+X → 拖拽选区 → Enter → 标注 → Ctrl+C → Esc
```

### 2. 精确截图
- 使用方向键微调选区
- 配合放大镜精确定位
- Shift+方向键快速移动

### 3. 批量截图
- 启用自动保存
- 连续截图自动保存
- 文件名自动编号

### 4. 临摹参考
- 截图后点击"贴图"
- 设置透明度50%
- 开启鼠标穿透
- 在下方绘图软件中临摹

### 5. 颜色提取
- 使用取色器
- 复制HEX值
- 直接用于设计

---

## 更新和支持

### 检查更新
- 托盘菜单 → 关于 → 检查更新
- 或访问官网下载最新版

### 获取帮助
- 用户手册：本文档
- 在线文档：https://vogadero.github.io/SnipX/
- GitHub：https://github.com/vogadero/SnipX
- Issues：https://github.com/vogadero/SnipX/issues

---

**感谢使用 SnipX！**

如有问题或建议，欢迎反馈。
