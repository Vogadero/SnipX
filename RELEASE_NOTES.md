# SnipX v1.1.5 - 交互、高 DPI 与编辑器性能优化

**发布日期**：2026-06-30

**适用平台**：Windows 7 / 8 / 8.1 / 10 / 11，推荐 Windows 10/11 x64

**许可协议**：MIT License

SnipX v1.1.5 聚焦截图编辑器、设置页、关于信息、滚动截图、录屏和高 DPI 体验。该版本补强了用户可见反馈，新增滚动截图和录屏的独立保存路径配置，改善高 DPI 环境下截图/取色器缩放观感，并优化模糊、马赛克标注和拖拽绘制时的重绘性能。

---

## 下载

| 文件 | 适用场景 | 说明 |
| --- | --- | --- |
| `SnipX.exe` | 便携版 | 下载后直接运行，无需安装 |
| `SnipX-1.1.5.zip` | 完整包 | 包含程序、README、用户手册和更新日志 |
| `SnipX-1.1.5-SHA256.txt` | 校验文件 | 用于校验发布资产完整性 |

---

## 本版本重点更新

### 设置与路径配置

- 修复设置窗口点击后无响应、关闭时影响主程序消息循环的问题。
- 设置页新增“录屏路径”和“滚动截图路径”，可分别自定义保存目录。
- 自动保存路径、录屏路径、滚动截图路径均可通过“浏览...”按钮选择。

### 滚动截图与录屏交互

- 开始滚动截图前会展示说明和当前保存目录。
- 滚动截图完成后会保存 PNG 到配置目录，并写入历史记录。
- 开始录屏前会展示说明和录屏保存目录，减少“是否已开始”的不确定感。
- 停止录屏后继续提示帧序列目录和生成的 GIF 路径。

### 关于与文档信息

- “关于”窗口展示当前版本号、项目说明和 GitHub 仓库地址。
- 支持从“关于”窗口直接打开 GitHub 仓库。

### 高 DPI、编辑器 UI 与性能

- 增加 Per-Monitor DPI 感知声明，并在程序入口尝试启用 Per-Monitor V2 DPI awareness。
- 改善高 DPI 环境下截图和取色器进入后观感像“分辨率变化”的问题。
- 优化截图编辑器在较窄截图下的最小窗口宽度，避免顶部工具按钮被裁切。
- 优化编辑器按钮悬停提示的字体、尺寸和颜色。
- 为模糊和马赛克标注增加局部渲染缓存，避免普通重绘时反复重新生成效果图。
- 移动、缩放和绘制标注时优先刷新旧区域与新区域的合并范围，减少整窗重绘开销。

---

## 快速开始

1. 从本 Release 下载 `SnipX.exe` 或 `SnipX-1.1.5.zip`。
2. 如果下载的是 ZIP 包，先解压到任意目录。
3. 双击运行 `SnipX.exe`。
4. 程序启动后驻留系统托盘。
5. 按 `Alt + X` 开始区域截图，或右键托盘图标使用滚动截图、录屏、设置等功能。

---

## 已知限制

- 滚动截图当前仍是简化实现，复杂应用、动态网页或非标准滚动控件可能无法完整拼接。
- 录屏当前以 PNG 帧序列为主，GIF 导出为基础占位能力。
- OCR 和上传入口仍为可扩展占位功能。
- 高 DPI、多显示器不同 DPI 的细节仍建议在真实设备上持续验证。

---

## 校验

Release 工作流会生成 `SnipX-1.1.5-SHA256.txt`。下载后可在 PowerShell 中使用：

```powershell
Get-FileHash .\SnipX.exe -Algorithm SHA256
Get-FileHash .\SnipX-1.1.5.zip -Algorithm SHA256
```

将输出值与 `SnipX-1.1.5-SHA256.txt` 中的 SHA256 值比对即可。

---

## 完整变更

- [CHANGELOG.md](https://github.com/Vogadero/SnipX/blob/main/CHANGELOG.md)
- [v1.1.5 提交历史](https://github.com/Vogadero/SnipX/commits/v1.1.5)
